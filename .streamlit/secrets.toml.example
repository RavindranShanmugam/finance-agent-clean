# Copy this file to `secrets.toml` (local) or paste into Streamlit Cloud → Settings → Secrets.
# OPTIONAL. Without it the deployed app uses the free keyword router and still works.
# Add it only if you want the public app to answer with real Claude AI.

# ANTHROPIC_API_KEY = "sk-ant-..."
# CLAUDE_MODEL = "claude-opus-4-8"   # optional; a smaller model is cheaper for a public demo
